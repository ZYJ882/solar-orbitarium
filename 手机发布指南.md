# 📱 手机发布指南 · 小白版

> 全程只需要一部手机 + 浏览器。**不用电脑、不用装软件、不用记任何命令。**
> 预计耗时 15 分钟（其中 5–8 分钟是等 GitHub 云端自动编译）。
> 完成后的效果：你的 GitHub 仓库 Releases 页面上，挂着一个可直接下载安装的 APK，
> 而且以后每次更新代码，都会**自动**重新编译、自动发布新 APK。

---

## ✅ 开始之前，确认 3 件事

1. 有 **GitHub 账号**（没有就去 github.com 免费注册一个）
2. 手机浏览器用 **Chrome**（其他浏览器也行）
3. 已经从这个平台下载了项目压缩包（见第 1 步）

---

## 第 1 步：下载项目压缩包 📦

在**你现在正在用的这个平台**的界面上，找这些按钮之一：

- 「**下载**」「**导出**」「**Export**」「**Download ZIP**」「**下载项目**」
- 通常藏在：右上角 `⋯` 菜单、顶部工具栏、或"文件"菜单里

下载后会得到 **1 个 `.zip` 文件**（名字可能类似 `project.zip`）。

> ⚠️ **重点：这个 zip 不要解压！保持原样直接上传！**
> （手机上解压反而会弄丢名为 `.github` 的关键隐藏文件夹）

---

## 第 2 步：创建公开仓库 🏠

1. Chrome 打开 **github.com**，登录
2. 页面太窄不好点？点 Chrome 右上角 `⋮` → 勾选「**桌面版网站**」
3. 页面右上角点 **`+`** → **`New repository`**
4. `Repository name` 填：**`solar-orbitarium`**
5. 选 **`Public`**（公开）
6. 其他选项**什么都不勾**，直接点绿色 **`Create repository`**

---

## 第 3 步：上传压缩包（关键一步）⭐

1. 进入刚建好的仓库，页面中间有蓝字 **`uploading an existing file`**，点它
2. 点 **`choose your files`**，选中**第 1 步下载的那个 zip 文件本身**
3. GitHub 会弹出提示，问是否解压（类似 *"Unzip project.zip?"*）→ 点 **`Unzip`（解压）**
4. 等它列出文件清单，**检查列表开头**：
   - ✅ **正确**：直接能看到 `.github`、`src`、`public`、`index.html`、`package.json` 等
   - ❌ **不对**：只看到一个文件夹、所有东西都套在里面 → **先别提交**，看文末「常见问题 ②」
5. 拉到页面最下方，点绿色 **`Commit changes`**

> 🎉 提交后，GitHub 云端会**自动开始编译 APK**，你什么都不用做。

---

## 第 4 步：确认自动编译开始了 👀

1. 仓库页面顶部点 **`Actions`** 标签
2. 看到有任务正在跑（黄色圆圈转动）→ ✅ 成功，去喝杯水 ☕
3. 如果 Actions 页面**空空如也** → 看「常见问题 ①」

---

## 第 5 步：下载 APK 📲

等待构建完成后：

1. 仓库页面 → 点 **`Releases`**
2. 打开顶部标记为 **Latest** 的版本发布页
3. 下载其中的 **`solar-orbitarium-v1.0.x.apk`**
   （版本号最大的安装包就是最新版）

> 💡 用 **GitHub 安卓 APP** 打开你的仓库，也能在 Releases 里看到并下载这个 APK。

---

## 第 6 步：安装 🎉

1. 打开刚下载的 apk 文件
2. 手机提示「未知来源应用」→ 选 **允许**（会跳到设置，给浏览器/下载器开权限）→ 返回继续安装
3. 桌面出现「**太阳系观测站**」图标 → 点开即可使用！

> 这个 App 完全离线可用，安装后断网也能正常演示，适合课堂使用。

---

## ❓ 常见问题

### ① Actions 页面是空的，没有自动编译

说明压缩包里缺了 `.github` 文件夹（或上传时没有选"解压"）。

- 回到仓库 → **`Add file`** → **`Upload files`** → 重新上传 zip，这次记得点 **Unzip**
- 还不行 → 看问题 ②

### ② 解压后所有文件都套在一个文件夹里

说明压缩包打包时多套了一层目录。手机上也能修：

1. 在仓库里依次点开：那个文件夹 → `.github` → `workflows` → `android.yml`
2. 点右上角 **`Raw`** → 长按页面 → **全选 → 复制**
3. 回到仓库**最外层** → **`Add file`** → **`Create new file`**
4. 文件名输入框里**完整输入**：`.github/workflows/android.yml`
   （输入 `.` 和 `/` 时，GitHub 会自动帮你创建文件夹）
5. 大文本框里**粘贴**刚才复制的内容 → 拉到底点 **`Commit changes`**
6. 回到 **`Actions`** 标签 → 左侧选 `android` → 点 **`Run workflow`** 手动触发一次

### ③ 找不到"桌面版网站"选项

- Chrome：右上角 `⋮` → 勾选「桌面版网站」
- 其他浏览器：设置或菜单里找「电脑版 / 桌面模式 / 请求桌面版网站」

### ④ 构建失败了（红色 ❌）

把失败页面**截图**发给我，我帮你看。绝大多数情况一次就能成功。

### ⑤ 以后想更新 App 怎么办？

改好代码 → 重新下载 zip → 仓库里重新上传、解压、提交 → 几分钟后 Releases 里**自动出现新版 APK**。

---

## 📋 项目文件清单（供核对）

| 文件 / 文件夹 | 作用 |
|---|---|
| `.github/workflows/android.yml` | ⭐ 自动编译 APK、自动发布到 Releases 的"总开关" |
| `src/` | 全部源代码（轨道模拟、行星档案、控制栏、数据对比） |
| `public/` | App 图标、PWA 配置、离线缓存脚本 |
| `index.html` / `package.json` / `package-lock.json` | 网页入口与依赖清单 |
| `vite.config.js` / `tsconfig.json` | 构建配置 |
| `capacitor.config.ts` | 安卓打包配置（App 名称、包名在这里） |
| `README.md` | 完整技术文档（含电脑端发布方式） |
| `手机发布指南.md` | 就是你正在看的这一份 |
